[package]
name = "fish-game-replay"
version.workspace = true
authors.workspace = true
edition.workspace = true
description = "Replay recording + cross-target verification for fish-game-core."

[lints.rust]
unsafe_code = "forbid"

[dependencies]
fish-game-core = { workspace = true }
serde = { workspace = true }
bincode = { workspace = true }
